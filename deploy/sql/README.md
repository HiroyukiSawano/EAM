# 数据库脚本说明

当前后端数据库结构统一交给 Flyway 增量迁移管理。

- 初始化脚本来源：`src/main/resources/db/migration`
- 首次部署：应用启动时自动执行迁移
- 升级部署：只新增 `V4/V5/...`，不要回改 `V1__init_schema.sql`

打包脚本会把当前仓库中的迁移 SQL 复制到发布目录 `sql/migration/`，用于：

- 运维审阅当前版本的迁移内容
- 排查线上迁移问题
- 在极端情况下人工比对脚本

正式部署时，优先以 Flyway 自动迁移为准，不建议手工跳过历史版本。
